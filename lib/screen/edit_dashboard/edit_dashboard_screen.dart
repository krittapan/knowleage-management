import 'package:flutter/material.dart';

class EditDashboardScreen extends StatelessWidget {
  const EditDashboardScreen({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('dashboard');
  }
}